// This file is kept for compatibility but direct database access is now handled by the backend
// All database operations should now go through the ASP.NET backend API

export const supabase = {
  // Placeholder to prevent import errors
  // Remove this file once all references are updated
};
